# Get server public IP address

With the following curl commands you can get the public IP address of the server for both IP v4 and v6:

## IP v4

```bash
curl -4 icanhazip.com
```

## IP v6

```bash
curl -6 icanhazip.com
```
