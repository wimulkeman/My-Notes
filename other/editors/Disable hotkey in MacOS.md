# Disable hotkey in MacOS

To disable the default MacOS behavior for CMD + SHIFT + A,
go to the following path:
```
Apple Menu > System Preferences > Keyboard > Keycombinations > Services > Text > Search in man page in Terminal
```